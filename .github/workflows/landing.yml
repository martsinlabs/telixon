name: Landing

on:
  pull_request:
    paths: ['apps/landing/**', 'scripts/check-links.mjs', '.github/workflows/landing.yml']
  push:
    branches: [main]
    paths: ['apps/landing/**', 'scripts/check-links.mjs', '.github/workflows/landing.yml']
  workflow_dispatch:

permissions:
  contents: read

# One landing pipeline per ref; abandon superseded preview runs, never a production deploy.
concurrency:
  group: landing-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # The live demos bundle @telixon/core and @telixon/web-sdk, so packages build first.
      - run: pnpm build

      - run: pnpm --filter @telixon/landing build

      - run: pnpm --filter @telixon/landing check:links

      - uses: actions/upload-artifact@v4
        with:
          name: landing-dist
          path: apps/landing/dist
          if-no-files-found: error

  deploy-preview:
    # Fork pull requests have no secrets; previews are for same-repo branches only.
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: landing-dist
          path: dist

      - uses: cloudflare/wrangler-action@v3
        id: deploy
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=telixon-landing --branch=${{ github.head_ref }}

      - uses: actions/github-script@v7
        with:
          script: |
            const url = '${{ steps.deploy.outputs.deployment-url }}';
            const marker = '<!-- landing-preview -->';
            const body = `${marker}\nLanding preview: ${url}`;
            const { data: comments } = await github.rest.issues.listComments({
              ...context.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find((comment) => comment.body.startsWith(marker));
            if (existing) {
              await github.rest.issues.updateComment({ ...context.repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ ...context.repo, issue_number: context.issue.number, body });
            }

  deploy-production:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: landing-production
      url: https://telixon.dev
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: landing-dist
          path: dist

      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=telixon-landing --branch=main
