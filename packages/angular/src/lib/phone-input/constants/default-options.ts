import type { TelixonPhoneInputOptions } from '../models';

/** The options a bare `telixonPhoneInput` attribute stands for. */
export const DEFAULT_PHONE_INPUT_OPTIONS: TelixonPhoneInputOptions = Object.freeze({ mode: 'international' });
