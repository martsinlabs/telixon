export const TYPE_ATTRIBUTE = 'type';

/** The `type` a trigger gets when its markup sets none, which keeps it out of form submission. */
export const BUTTON_TYPE = 'button';

/** The `tabindex` of the listbox. A scrollable list would otherwise become a tab stop in Chromium. */
export const OUT_OF_TAB_ORDER = -1;
